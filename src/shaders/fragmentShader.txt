#version 400 core

in vec3 color;

out vec4 out_Color; 

void main(void){
	// just straight up use our RGB value (which will be interpolated based on
	// the position of the pixel relative to the vertices making
	// up the triangle
	out_Color = vec4(color,1.0); // alpha component = 1

}